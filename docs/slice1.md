Define a simple first slice of a React program to show a department course schedule.

The first slice should have the component App use JSX to show the title of the schedule for 2018-2019. Use TypeScript. Render plain HTML, no fancy CSS. Use the data stored in the following code:

```
const schedules = {
  "CS-2018-2019": {
    title: 'CS Courses for 2018-2019'
  }
}
```
